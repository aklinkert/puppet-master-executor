version: "3"
services:
  executor:
    image: registry.gitlab.com/scalifyme/like/executor
    build: .
    networks:
      - like
    environment:
      - "NODE_ENV=dev"
      - "CHROME_HEADLESS=true"

networks:
  like:
    external: true
